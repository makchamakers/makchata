'use client';

import styled from 'styled-components';

export default function Summary() {
  return <Container>한눈에보기</Container>;
}

const Container = styled.main`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
`;
